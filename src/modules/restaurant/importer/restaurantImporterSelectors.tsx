import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const restaurantImporterSelectors = importerSelectors(
  'restaurant.importer',
);

export default restaurantImporterSelectors;
