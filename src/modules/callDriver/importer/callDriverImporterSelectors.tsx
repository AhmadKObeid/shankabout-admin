import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const callDriverImporterSelectors = importerSelectors(
  'callDriver.importer',
);

export default callDriverImporterSelectors;
