import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const customerAdressImporterSelectors = importerSelectors(
  'customerAdress.importer',
);

export default customerAdressImporterSelectors;
