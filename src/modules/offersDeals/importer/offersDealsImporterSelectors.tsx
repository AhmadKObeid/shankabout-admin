import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const offersDealsImporterSelectors = importerSelectors(
  'offersDeals.importer',
);

export default offersDealsImporterSelectors;
